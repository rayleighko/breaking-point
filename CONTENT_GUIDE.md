# 콘텐츠 작성 규격

랩 하나를 쓸 때마다 이 문서를 열고 체크리스트처럼 씁니다.
일관성이 이 프로젝트의 가장 큰 자산입니다.

## 주제 위키의 고정 구조

주제 설명은 `src/content/topics/<slug>.json`에서 한 파일씩 관리합니다. `src/content.config.ts`의 schema가
분류, 출처와 연결 구조를 build time에 검증합니다.

- `kind`에 따라 상세 페이지의 첫 질문이 달라집니다. 현상, 시스템, 개발 방식과 Product 용어를 모두
  `어떤 현상인가요?`로 일반화하지 않습니다.
- `description`은 정의만 쓰지 않고 언제 필요하며 무엇과 혼동하기 쉬운지 설명합니다.
- `signals`는 장애 신호뿐 아니라 그 개념을 떠올려야 하는 코드·조직·설계 상황을 포함할 수 있습니다.
- `related`는 존재하는 topic slug만 사용합니다. 단순히 함께 읽으면 좋은 관계에만 씁니다.
- 학습 순서나 차이를 설명해야 하는 연결은 `relations`에 `선행 개념`, `비교하기`, `구분하기`,
  `적용 사례` 중 하나와 연결 이유를 함께 적습니다. 이유 없는 링크만 늘리지 않습니다.
- backlink는 다른 topic의 `related`와 `relations`에서 build time에 계산합니다. 양쪽 파일에 같은 연결을
  중복해서 적지 않습니다.
- `keywords`가 기존 topic 이름과 같으면 내부 위키로 연결합니다. 아직 topic이 없으면 외부 검색으로
  연결되며, 검색 결과는 검증된 출처가 아닙니다. 근거는 반드시 `sources`에 따로 등록합니다.
- 이름이 있는 방법론과 표준은 `sources`에 원전 또는 공식 문서를 포함합니다.
- 표준화되지 않은 실무 용어와 회사 사례는 `context.standing`, `attribution`, `note`로 누가 어떤 맥락에서
  사용했으며 어디까지 일반화할 수 있는지 표시합니다.
- 한 주제 개선은 가능한 한 해당 JSON 파일 하나와 검증 변경으로 제한합니다.

## 랩의 고정 구조

순서를 바꾸지 않습니다. 독자가 두 번째 랩부터는 구조를 예측할 수 있어야 합니다.

| #   | 섹션                         | 목적                                        | 분량         |
| --- | ---------------------------- | ------------------------------------------- | ------------ |
| 1   | **일상 비유**                | 개념을 아는 사람 0명 기준으로 시작          | 3~5문단      |
| 2   | **직접 돌려보세요**          | 시뮬레이터 + "이것만은 해보라"는 지시       | 시뮬 + 2문단 |
| 3   | **왜 이렇게 되는지, 산수로** | 공식 하나. 곱셈/나눗셈 수준                 | 표 1개 포함  |
| 4   | **흔한 오해**                | "그냥 ○○하면 되잖아요?"에 정면으로 답       | 3~4문단      |
| 5   | **챌린지**                   | 제약이 있는 문제. 순진한 해법은 실패해야 함 | 시뮬         |
| 6   | **확인 문제**                | 4문항. 오답에도 이유를 설명                 | Quiz         |
| 7   | **30초 리마인드**            | 공식 1개 + 문장 5개 이하                    | Recall       |
| 8   | **현실에서는**               | 실제 설정값·코드·수치                       | 코드블록 1~2 |
| 9   | **다음 랩**                  | 왜 다음 주제로 이어지는지 한 문단           | 1문단        |

## 문장 규칙

- **한 문장에 개념 하나.** 두 개가 들어가면 자릅니다.
- **용어는 현상 뒤에 붙입니다.** ("줄이 끝없이 길어집니다. 이걸 큐 폭발이라고 부릅니다." ⭕ / "큐 폭발이란 …" ❌)
- **숫자는 계산 가능한 크기로.** 200 × 0.05 = 10 처럼 암산되는 값을 씁니다.
- **영어 약어는 첫 등장에 풀어씁니다.** `p99(느린 쪽 1%의 응답시간)`
- 과장된 표현("혁명적인", "반드시")을 쓰지 않습니다. 시뮬레이션이 대신 말하게 둡니다.
- 기술 용어와 고유명사는 업계에서 검색 가능한 공식 표기를 씁니다. (`Kubernetes`, `AWS Lambda`, `PostgreSQL`)
- vendor 제품을 먼저 정답처럼 제시하지 않습니다. **Generic pattern → concrete implementation** 순서로 씁니다.
- 글로벌 사례는 [`docs/GLOBAL_STANDARD.md`](./docs/GLOBAL_STANDARD.md)에 따라 국제 표준, de facto
  standard, vendor behavior, industry practice와 company case를 구분합니다.

### 사람이 실제로 쓰는 용어

- 사전이나 논문의 직역어보다 한국 개발 현장에서 실제로 말하고 검색하는 표현을 우선합니다.
- 번역어가 낯설면 `Queue(대기열)`처럼 보편적인 원어와 자연스러운 한국어를 함께 씁니다.
- 학술 번역어가 필요한 경우 본문에서 적용 맥락을 설명합니다. 일반 화면의 대표 이름으로 먼저 쓰지
  않습니다.
- 영어를 그대로 쓰는 것과 기계 번역투를 피하는 것은 별개의 문제입니다. 문장 구조는 자연스러운
  한국어로 쓰고, 정확성과 검색 가능성이 필요한 technical term만 원어를 유지합니다.
- 새 용어는 실제 채용 공고, 공식 한국어 문서와 국내외 engineering 발표에서 어떤 표현이 쓰이는지
  확인합니다. 출처 한 곳의 표현만으로 보편적 용례라고 단정하지 않습니다.
- `~됩니다`, `~할 수 있습니다`를 반복해 문장을 부풀리지 않습니다. 사람이 동료에게 설명하듯 짧고
  구체적으로 씁니다.

## Technology lens

`현실에서는` 섹션은 관련 있는 범위에서 다음 질문에 답합니다. 자세한 기준은
[`docs/TECHNOLOGY_LENSES.md`](./docs/TECHNOLOGY_LENSES.md)를 봅니다.

- TypeScript application stack에서는 어디에서 이 현상이 보이는가?
- Docker container와 Kubernetes Pod를 늘리면 무엇이 해결되고 무엇이 남는가?
- Serverless에서는 서버 관리가 사라진 대신 어떤 concurrency limit이 생기는가?
- AWS managed service로 옮기면 어떤 component가 같은 책임을 맡는가?
- overload가 오면 queue 상한, timeout, retry, load shedding을 누가 책임지는가?

## 시뮬레이터 규칙

- **첫 화면에서 이미 뭔가 움직이고 있어야** 합니다. 시작 버튼을 누르게 하지 않습니다.
- 컨트롤 라벨은 **일상어 + 기술 용어** 두 겹으로 답니다. → `창구 개수` / `커넥션 풀 크기`
- 프리셋 버튼을 3~5개 둡니다. 그중 최소 하나는 **직관에 반하는** 상황이어야 합니다.
  (예: 트래픽은 그대로인데 쿼리만 느려져서 죽는 경우)
- 판정 배너는 항상 **사람 말로** 씁니다. "ρ=1.25" ❌ / "들어오는 속도가 처리 속도보다 빠릅니다" ⭕
- 시드를 고정해 **재현 가능**하게 만듭니다.
- CS 알고리즘과 자료구조 시각화는 [`docs/CS_VISUALIZATION.md`](./docs/CS_VISUALIZATION.md)의
  상태, 다음 결정, 누적 비용, 불변식과 현실 연결을 함께 보여줍니다.

## 챌린지 규칙

좋은 챌린지의 조건 — **순진한 해법이 반드시 실패해야 합니다.**

- 자원을 늘리는 방향에 **상한**을 둡니다. (예: 커넥션 20개까지)
- 정답이 여러 개면 좋습니다. 다만 전부 그 랩의 핵심 원리를 써야 합니다.
- 실패했을 때 **왜 실패했는지 구체적인 숫자로** 알려줍니다. 그냥 "실패"는 배움이 없습니다.
- 통과했을 때 한 줄 교훈을 남깁니다.

## 접근성

- 색만으로 상태를 표현하지 않습니다 (색 + 아이콘 + 텍스트).
- 캔버스에는 `role="img"`와 설명적인 `aria-label`을 답니다.
- `prefers-reduced-motion`을 존중합니다 (전역 CSS에 이미 처리돼 있음).
- 컨트롤은 키보드로 조작 가능해야 합니다 (기본 `<input>` 요소 사용).

## 발행 전 체크리스트

- [ ] 이 주제를 처음 듣는 사람이 1번 섹션만 읽고 상황을 그릴 수 있는가
- [ ] 시뮬레이터를 안 만지고 글만 읽어도 말이 되는가 (그리고 만지면 더 좋은가)
- [ ] 챌린지에서 "자원만 늘리는" 해법이 실패하는가
- [ ] 리마인드 카드만 읽어도 핵심이 남는가
- [ ] `현실에서는` 섹션의 설정값이 실제 문서와 맞는가 (출처 확인)
- [ ] vendor보다 generic pattern을 먼저 설명했고, 공식 명칭과 1차 출처를 사용했는가
- [ ] overload 시 책임 주체와 안전장치를 설명했는가
- [ ] 이름이 있는 개념은 원전, 표준 또는 공식 문서까지 추적했는가
- [ ] 사실, 경험적 조언과 개인의 관점을 문장에서 구분했는가
- [ ] 외부 문제·해설·도표를 복제하지 않고 scenario, 수치와 시각화를 독자적으로 만들었는가
- [ ] “해외에서는”으로 일반화하지 않고 표준의 종류, 적용 범위와 지역별 constraint를 표시했는가
- [ ] 번역어가 아니라 실제 개발자가 말하고 검색하는 용어인지 확인했는가
- [ ] 소리 내어 읽었을 때 기계 번역이나 문서 template처럼 어색한 문장이 없는가
- [ ] `src/lib/roadmap.ts` 의 status / slug 를 갱신했는가

출처의 등급과 저작권 기준은 [`docs/KNOWLEDGE_SOURCES.md`](./docs/KNOWLEDGE_SOURCES.md)를 따릅니다.
